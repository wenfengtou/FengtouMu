# 开发日志

> 项目：**FengtouMu** — ESP32 离线仿真 IDE（Tauri 2 + Rust + React，复用 PICSimLab QEMU 动态库）
> 仓库：<https://github.com/wenfengtou/FengtouMu>
> 本文按时间顺序记录实际发生的开发活动、关键决策与踩坑。

---

## 2026-09-05 · QEMU 动态库打通

- 从 `lcgamboa/qemu` 的 `picsimlab-esp32` 分支（QEMU 9.2.2 基线）编译出 **`libqemu-xtensa.dll`**，
  取代此前的 exe 形态，供 PICSimLab / 自研 IDE 以 `LoadLibrary` 方式调用。
- 关键工作：在 `system/cpus.c` 增加 iothread 锁兼容别名
  （`qemu_mutex_lock_iothread_impl` / `qemu_mutex_unlock_iothread`），
  以适配预编译版 PICSimLab 0.9.2 按旧符号名解析的行为。
- 验证：PICSimLab 加载成功；GPIO 演示固件 LED 闪烁、BOOT 按键控制均正常（回调链路完整）。

## 2026-09-06 · 项目立项，M1 MVP 完成

- 搭建 `esp32-ide` 工程骨架：**Tauri 2 + React + TypeScript + Monaco Editor**。
- 完成 M1 范围：
  - Rust 侧 DLL 封装（`libloading` + `#[repr(C)]` 回调结构体，21 个符号核验）；
  - 固定板卡视图（ESP32 DevKitC，板载 LED = GPIO2 / BOOT 按键 = GPIO0，38 引脚状态可视化）；
  - 串口终端（UART0 收发）；
  - 编译链封装（调用 arduino-cli，产出 4MB 合并镜像 + efuse 文件）。
- **踩坑 1：固件编译参数**
  用 Arduino 默认 fqbn（80MHz/QIO）编译的固件在 QEMU 下 flash 初始化必失败
  （日志 `qio_mode: Failed to set QIE bit` → `assert failed: init_flash`）。
  改为 **`FlashMode=dio,FlashFreq=40,FlashSize=4M,PartitionScheme=default,PSRAM=disabled`**
  后立即通过。该参数已固化为编译链默认值。
- **验证**：新增无 GUI 集成测试 `src-tauri/tests/sim_integration.rs`，
  覆盖 `DLL 加载 → 仿真启动 → GPIO 回调 → LED 闪烁` 全链路，实测 LED 翻转正常。
- **踩坑 2（引出架构改造）**：PICSimLab 的 QEMU 构建**不支持同进程二次 `qemu_init`**
  （第二次会耗尽静态区直接退出进程），且**不能安全卸载**（`FreeLibrary` 后残留线程访问已释放代码，
  表现为点击"运行"闪退 0xC0000005）。

## 2026-09-11 · 子进程架构改造 + UI 自动化

- **架构改造：仿真移入独立子进程**
  - 新增二进制 `esp32-sim`（`src/bin/sim_worker.rs`）：独占加载 QEMU DLL，通过
    stdin/stdout 的 JSON Lines 协议与主进程通信；
  - 主进程改为"宿主"（`src/sim/worker_host.rs`）：spawn 子进程、转发事件、健康检测、
    卡死自动重试、命令代理。主进程**不再加载 DLL**。
  - 收益：可无限次重复运行；冷启动卡死时杀掉子进程换新进程重试；子进程异常不影响界面，**不再闪退**。
- **健康判据迭代**（避免把 ROM 卡死误判为"启动成功"）：
  - 初版"任意引脚变化"过于宽松（卡死前也可能有一两次抖动）；
  - 二版改为"引脚活动持续：累计 ≥2 次且最近 2 秒内仍有活动，或 UART 输出 ≥1500 字节"；
  - 三版（M2 期间收紧，见下）要求"累计 ≥4 次、首次活动距今 ≥3 秒、最近 2 秒内仍有活动，或 UART ≥1500 字节"——QEMU 在 ROM 阶段抖几下后卡死的情形不再被误判为成功。
- **UI 自动化**：新增 `scripts/ui_drive.mjs`，通过 WebView2 远程调试（CDP）驱动**真实界面**：
  自动加载 DLL → 点击"运行"（内部自动重试）→ 采样板卡 LED 翻转 → 点击"停止" → 校验子进程退出。
- **构建脚本**：新增 `scripts/build_release.ps1`（一键：编译主程序 + 仿真子进程 → 同步打包资源 →
  `tauri build` 产出 exe / MSI / NSIS）。
- **验证**：同一应用会话内连续两次 `运行 → LED 闪烁（15 秒 19/18 次）→ 停止` 全部通过，全程无闪退。

## 2026-09-12 · 仓库整合与 DLL 重编

- **`wenfengtou/qemu`（GitHub fork）**：将此前在 `myself/qemu` 上的 7 个提交
  （WiFi AP 单播桥接修复、MSYS2/mingw 构建修复、FromDS 修复、iothread 锁别名、开发文档等）
  cherry-pick 到该 fork，并补充 1 个文档提交，已推送。
- **目录重命名**：`esp32-ide` → **`FengtouMu`**，代码中硬编码路径（`App.tsx` 默认 fw/输出目录、
  集成测试资源路径）同步更新。
- **建立项目仓库**：`FengtouMu` 初始化 git 并推送到
  <https://github.com/wenfengtou/FengtouMu>（`main` 分支）。
- **从 fork 重新编译 DLL**（`docs/build-qemu-dll-windows.md` 记录了完整步骤）：
  - **踩坑 3**：`--enable-debug` 会产出 `-O0 + debug_tcg` 的调试版 QEMU，
    实测固件启动后崩溃（EXCCAUSE）、且大量冷启动卡死。正确配置为
    `--target-list=xtensa-softmmu --enable-slirp --enable-gcrypt --disable-werror --disable-pie --disable-docs --disable-plugins`（即 `-O2`、关闭 debug_tcg/plugins）。
  - **踩坑 4**：由 exe 响应文件重链 DLL 时必须加 **`-Wl,--export-all-symbols`**；
    否则 MinGW 不会把全局符号写入 PE 导出表，表现为"DLL 能加载但 `qemu_init` 找不到"。
  - 产物：`libqemu-xtensa.dll`（68.5 MB），17 个关键符号导出齐全。
- **验证与部署**：集成测试 + UI 自动化双通过；新 DLL 已部署到
  `FengtouMu/lib/qemu/`（IDE 使用）与 `PICSimLab/picsimlab_win64/lib/qemu/`。
- **收敛**：删除 `myself`（2.1 GB）——其改动已全部迁入 fork，DLL 亦不再依赖该目录。

## 2026-09-12 · M2 起步：电路图编辑器与元件

- 前端状态分层（M2-2）：新增 `src/state/`，按域拆出界面、仿真、编辑、电路四组状态（轻量 store，无新增运行时依赖），并新增 `bridge.ts` 负责引脚变化 → 元件外观、用户操作 → 引脚注入；`App.tsx` 只保留装配逻辑，串口终端改为受控组件。
- 引脚与网表抽象（M2-1）：新增 `src/circuit/`，含数据模型、元件目录、网表构建（导线合并 + 电阻透传）、行为计算与图纸读写；底板引脚表与 Rust 侧 `DEVKITC_PINMAP` 对齐（板级 24 = GPIO2、25 = GPIO0）。
- SVG 画布（M2-3）：放置、拖拽、选中、删除、点击两个引脚连线、滚轮缩放、空白拖动平移；元件与引脚带 `data-part`、`data-pin`、`data-wire`、`data-lit`（LED）、`data-pressed`（按键）、`data-pot`（电位器）、`data-board-led`（板载 LED）等属性，便于自动化定位与断言。
- 图纸读写（M2-4）：`diagram.json` 对齐 Wokwi 格式（parts/connections），导入兼容 Wokwi 引脚别名（电阻 `1/2`、按键 `1.l/2.r`、电位器 `1/2/3`），并对未知类型、重复 id、非法引脚给出可读错误；配套新增 Rust 命令 `cmd_read_text_file` / `cmd_write_text_file`。
- 元件（M2-5）：LED（由 GPIO 驱动）、电阻（网表内透传，支持"GPIO → 电阻 → LED → GND"接法）、按键、电位器（经新增 `cmd_set_apin` 注入 12 位 ADC 读数，通道表见 `src/circuit/behavior.ts`）。
- 验证结果（`npm run build:release` + 26 项单元测试 + 4 条 UI 自动化链路全部通过）：
  - 前端单元测试 26 项通过（`npm run test:unit`）：网表 4、行为 9、图纸 9、目录 4。
  - UI 自动化（真实鼠标事件，`npm run test:ui`，共 4 条链路）：A 板卡视图 LED 翻转 49 次；B 电路图放置 LED → 连线 GPIO2/GND → 运行 → 画布 LED 翻转 32 次；C 放置按键 → 连线 GPIO0/GND → 运行 → 按住时 `data-pressed=1`；D 放置电位器 → SIG 接 GPIO34 → 运行 → 拖动旋钮 → 读数 0.5 → 1.0 且注入无报错。
  - Rust 侧新增 `tests/button_injection.rs`，验证注入 API 与 QEMU 同步回调链路。
  - 发布产物：`src-tauri/target/release/esp32-ide.exe` 与 MSI/NSIS 安装包。
- 验证期间发现并修复的问题：
  - **启动健康判据过松**：QEMU 在 ROM 阶段抖出两次引脚事件后卡死，判据即判定"启动成功"，表现为界面显示运行中但 LED 一次都不翻转。现改为"累计 ≥4 次、首次活动距今 ≥3 秒且最近 2 秒内仍有活动，或 UART ≥1500 字节"，实测启动耗时 6.7–10.6 秒（真实启动），ROM 卡死会被识别并自动换进程重试。
  - **按键可点击区域过小**：此前只有按键外壳 `<rect>` 响应按下，点中"按键"标签（元件中心）不会触发。现整块元件任意位置都可按下（引脚自身的 `mousedown` 会 `stopPropagation`，因此接线不会误触发）。
  - **UI 自动化落点探测**：画布上元件会互相压盖（新增元件按 `y = 80 + (n-1) × 50` 叠加）、导线也可能横穿元件。脚本改用 `document.elementFromPoint` 逐候选点探测真正命中目标元件的落点；同时每条链路开始前先确保仿真已停止，避免上一条链路失败残留运行态干扰后续断言。
- 引擎侧的两个发现：
  - QEMU 每次读取 GPIO 输入寄存器都会通过 `picsimlab_dir_pin(-1, ...)` 请求同步，此前我们忽略了该请求。现已按 PICSimLab 的做法，把外部驱动的电平重新推回模型（`SET_PIN_FN` + `PinTable::forced_inputs`）。
  - 已知限制：~~固件以 `INPUT_PULLUP` 读取引脚时，注入的低电平无法稳定保持~~ —— **该记录已于 2026-09-12 查实为误诊**（固件产物被空草图覆盖 + 测试采样时机错误 + 引擎串口首字节丢失），见下方排查章节；模型侧保持机制工作正常。

## 2026-09-12 · M3 起步：工程文件、环境自检与 CI（M3-1 / M3-2 / M3-3）

- **工程文件（M3-1）**：新增 `.fmp` 工程格式（版本号 + 名称 + 源码 + `diagram.json` 文本 + 草图/fw/固件路径 + fqbn）。Rust 侧 `project.rs` 只做「存取 JSON」，电路语义仍留在前端。
  - 命令：`cmd_project_save/load`、`cmd_prefs_load/save`（最近工程列表与上次工程写入应用数据目录 `prefs.json`）、`cmd_app_paths`。
  - 前端 `src/state/projectStore.ts`：新建 / 打开 / 保存 / 另存为 / 最近工程 / 恢复上次；内容变化自动标记「未保存」，每 30 秒把改动写进 `autosave.fmp`，重启后可从工程工具条「恢复上次编辑」找回。
  - 工程格式与解析放在 `src/project/format.ts`（纯函数）：版本过新、非 JSON、既无 `code` 又无 `diagram` 的输入都会被拒绝并给出可读原因。
- **Wokwi zip 互导（M3-1）**：`cmd_export_wokwi_zip` 导出 `diagram.json` + `sketch.ino`；`cmd_import_wokwi_zip` 解压到应用数据目录，并按 arduino-cli 的「目录名与 .ino 同名」要求自动整理出可编译的 sketch 目录（整理结果写进导入提示，不静默处理）。
- **环境自检（M3-2）**：`envcheck.rs` 检查 7 项 —— QEMU 动态库（含体积与 glib/gcc/pixman 依赖）、fw 目录（ROM + keymaps）、仿真子进程、arduino-cli（版本号）、ESP32 核心（`arduino-cli core list`）、固件镜像、应用数据目录可写；每项给出 `ok / warn / missing` 与「可执行的下一步」。前端 `EnvPanel` 以面板呈现，启动时有缺失会自动弹出，并可直接在面板里改 fw 目录 / 固件镜像后重新检查。
- **CI（M3-3）**：`.github/workflows/ci.yml`，Windows 上两个 job —— 前端 `npm ci → npm run build → npm run test:unit`；Rust `cargo check --bins --tests → cargo test`（复用前端上传的 `dist` 产物，因为 `tauri::generate_context!` 要求 `frontendDist` 目录存在）。依赖本机 DLL 的 `sim_integration` / `button_injection` 标记 `#[ignore]`，新增的 `project_io`、`envcheck` 用例不需要 DLL，可在 CI 稳定运行；本地全量用 `npm run test:rust:local`。
- 验证结果：
  - 前端单元测试 47 项通过：新增 17 项工程格式用例（组装/命名/解析拒绝/最近列表去重截断/内容等价）与 4 项「工程 ↔ 编辑器/电路」胶水用例（灌入后各域状态正确、`snapshot` 往返对称、diagram 为空时清空电路、非法引脚给出可读错误）。
  - Rust 测试：`project_io` 3 项（工程文件往返、Wokwi zip 往返、缺件 zip 提示）、`envcheck` 4 项（缺 DLL 指引、依赖判定、fw 目录判定、固件镜像状态）、`buildchain` 4 项（产物命名与前置校验）全部通过；3 个需要本机工具链的用例按预期跳过。
  - UI 自动化扩为 8 条链路（新增「工程新建」、「环境自检」、「自动保存恢复」、「一键编译」），全部通过。
  - `npm run build:release` 产出 exe 与 MSI/NSIS 安装包。
- 顺手解决的两个坑：
  - **`bundle.resources` 在编译期就校验**：`tauri.conf.json` 里声明的 `resources/esp32-sim.exe` 是构建产物（已 gitignore），但 `tauri-build` 每次编译都会校验其存在 —— 全新克隆直接 `cargo check/test` 会报「resource path doesn't exist」。现在 `build.rs` 在缺失时先打印一句可执行的提示，CI 则放一个空文件占位（不执行、仅满足校验），README 也注明了要「先 build:release 再跑 cargo」。
  - **自动保存的会话提示不消失**：保存 / 打开 / 新建后原先不会清掉会话标记，「恢复上次编辑」按钮会一直挂着；现在这些动作会清掉会话，并补了一个「忽略」按钮（同时清空 `autosave.fmp`，避免下次启动又被提示）。
- 顺带清理：示例源码常量从组件 `CodeEditor.tsx` 挪到 `src/project/defaults.ts`，让状态层不再依赖 UI 组件（也让工程胶水的单元测试可以脱离 Monaco 运行）。

## 2026-09-12 · 修复：一键编译「exit 0 却报编译失败」

- 现象：点「编译」后提示 `编译失败: 编译失败（arduino-cli exit Some(0)）: Sketch uses 285316 bytes (21%) …`，而 arduino-cli 的退出码是 0、输出也是成功的体积统计。
- 根因：`buildchain.rs` 用 `sketch_dir.file_stem()` 拼产物名 —— 目录 `picsimlab_gpio_demo` 的 `file_stem()` 是 `picsimlab_gpio_demo`，于是去找 `picsimlab_gpio_demo.merged.bin`，而 arduino-cli 实际产出的是 `picsimlab_gpio_demo.ino.merged.bin`；找不到就落进"失败"分支，还把成功的 stdout 当错误信息一起打了出来。这条路径此前没被真正验证过（M1 的固件是手工编译+合并准备好的）。
- 修复：
  - 新增 `sketch_name()`：优先取与目录同名的 `.ino`，否则取目录内任意 `.ino`（并据此给出"目录名与 .ino 需同名"的直白提示）。
  - 新增 `find_merged_bin()`：按 `<sketch>.ino.merged.bin` → `<目录名>.merged.bin` → 输出目录里最新的 `*.merged.bin` 依次定位，不再依赖单一命名。
  - 退出码非 0 才叫失败；退出码为 0 但找不到合并镜像时，错误信息里列出输出目录与已有产物，并提示保留 `FlashMode=dio,FlashFreq=40,FlashSize=4M`。
  - 前置校验移到「调用 arduino-cli」之前：目录不存在 / 没有 `.ino` / 目录名与 `.ino` 不同名都能立刻给出可读原因。
- 验证：
  - 新增 `src-tauri/tests/buildchain.rs`：4 项不需要 arduino-cli 的用例（命名优先级、`<sketch>.ino.merged.bin` 命中、兜底命名与空目录、前置校验提示）；另 1 项 `#[ignore]` 用例真实调用 arduino-cli 编译 demo 草图，实测产出 `picsimlab_gpio_demo.ino.merged.bin`（4194304 字节）。
  - UI 自动化新增「H 一键编译」链路：真实点击「编译」→ 断言出现"正在编译"→"编译成功"，且固件路径切到 `*.ino.merged.bin`。

## 2026-09-12 · 修复：编译编的不是编辑器里的代码（改了代码"没生效"）

- 现象：用户打开工程 `lwf.fmp`，把代码改成 5 秒闪烁并加串口打印，点「编译 → 运行」后闪烁周期不变、串口也没有预期的打印。
- 根因：编辑器里的代码只存在内存 / 工程文件里，而 arduino-cli 编译的是**磁盘上的草图目录** —— 两者不同步时，编出来的还是旧固件。`lwf.fmp` 的 `sketchDir` 指向演示目录，磁盘上的 `.ino` 仍是 500ms 演示版，所以"改 5s 没生效"；串口其实有输出，只是那是旧固件的打印，且 QEMU 的 UART 按波特率模拟、输出明显滞后（见下方已知限制），很容易被误认为"没有日志"。
- 修复：
  - `compile()` 新增 `code` 参数：编译前经 `sync_sketch_source()` 把编辑器内容写入 `<sketchDir>/<sketch>.ino`。内容一致时不写盘；首次覆盖时把原文件备份为 `<sketch>.ino.bak`（只备份一次，不覆盖已有备份）。
  - 成功提示注明「已把编辑器内容写入 …（原文件已备份为 …）」；前端 `compileCurrent()` 把当前编辑器内容与工程 fqbn 一起传给编译链。
- 验证：
  - 新增 `src-tauri/tests/user_sketch_blink.rs`（`#[ignore]`，本地跑）：临时草图先放 500ms 旧固件，用 5 秒新代码编译 → 断言磁盘 `.ino` 已被更新、存在备份、合并镜像为 4MB → 起仿真实测 **LED 相邻翻转间隔 ≈ 4.99–5.00 秒**（多次运行均如此），证明编出来的确实是编辑器里的代码。
  - `buildchain` 新增 `sync_sketch_source` 单元用例（内容一致不写盘、覆盖时备份一次、不重复备份）。
  - UI 自动化「H 一键编译」增加断言：编译成功提示里必须出现「编辑器内容已同步到草图目录」。
- 已知限制（与"看不到串口日志"直接相关）：
  - QEMU 的 UART 按波特率模拟，输出明显滞后于 CPU 时间：固件 setup 里的 `Serial.println` 可能要等 20–40 秒才在终端出现，ROM 启动日志也是逐段冒出来的。
  - 仿真运行约 20 秒后，本固件会偶发崩溃（Guru Meditation）进入重启循环，极端情况下 QEMU 进程会直接中止 —— 与早期 demo 固件的崩溃行为一致，属模型侧问题，待后续定位（M2 遗留的按键保持问题同源）。

## 2026-09-12 · M3-4 协议级测试矩阵

- **设计**：矩阵分四层 —— A 线协议 + 宿主状态机（用假 worker 替身走真实进程链路，CI 可跑）、B 引脚状态表行为（CI 可跑）、C 真实 QEMU 行为（`#[ignore]`，本机跑）、D 前端网表快照（CI 可跑）。用例编号与完整清单见 `docs/仿真协议.md`。
- **新增代码**：
  - `src-tauri/src/sim/protocol.rs`：JSON Lines 线协议的帧构造与解析（6 项单测）。
  - `src-tauri/src/bin/fake_worker.rs`：协议替身，用环境变量编排行为（事件脚本、死亡时机、无响应、致命错误），通过宿主既有的 `ESP32_IDE_SIM_WORKER` 环境变量接入。
  - `SimHost::with_bus()`：注入自定义事件总线，让宿主可脱离 GUI 测试；`worker_host::health_pass` 提取为纯函数。
  - `tests/worker_protocol.rs`（9 项：健康启动/判据矩阵/重试/重试耗尽/致命错误/命令往返/停止/无响应超时）、`tests/pins_matrix.rs`（6 项：默认态/输出/输入驱动/方向/复位/越界）、`tests/behavior_matrix.rs`（1 项组合：固件冒烟/UART 收发/多引脚并发写/串口批处理，`#[ignore]`）。
  - 前端 `src/circuit/__tests__/snapshot.test.ts`：标准教学电路的网表 golden snapshot（2 项）。
- **验证**：`npm run test:rust` 全绿 —— 协议 6 + 工作进程 9 + 引脚 6 + 编译链 5 + 环境 4 + 工程 3 = **33 项 CI 可跑**，5 项需本机工具链的用例按预期跳过；`npm run test:unit` 49 项通过（含快照）。
- **顺手修掉一个真实崩溃**：排查 C 层用例"必崩"时，从 Windows 事件日志定位到 `esp32-sim.exe` 反复以 `0xc0000602`（fail-fast）崩在 **combase.dll** 同一偏移 —— QEMU 每次启动都会开 VNC 服务器，其初始化在 Windows 上触发 COM fail-fast。已给引擎 argv 加 `-vnc none`：消除崩溃源、释放 5900 端口、省掉 VNC 的 CPU，且本应用根本不用 VNC（界面是自绘 SVG，GPIO/UART 走回调）。加 `-vnc none` 后，C 层两条真实 QEMU 用例均通过：`behavior_matrix`（固件冒烟 / UART 上行 / 多引脚并发写，25.7s）与 `user_sketch_blink`（**5.000s 翻转间隔**，31.7s）。
- **已知限制**（与"仿真不稳定"直接相关，属模型侧、待定位）：该 QEMU 模型对宿主负载敏感 —— 独立探测时固件可正常启动并闪烁（21 次/25s），但在应用内（叠加 WebView2/CDP/arduino-cli 的负载下）会偶发两种故障：冷启动停摆（ROM 日志停在 `load:...len:3164`，健康判据自动重试）或进入应用后非法指令崩溃（Guru Meditation，EXCCAUSE 7）。UI 自动化 8 条链路今晨全部通过过一次；模型不稳定时 A/B 会失败、且 worker 硬崩后主界面可能卡住 —— 这也是 M2 遗留按键保持问题同源的模型侧症状，后续从 `wenfengtou/qemu` 的 esp32 机型侧继续定位。

## 2026-09-12 · M2（续）：元件库扩充、自动布局与 Wokwi 互导边界补强

- **元件库扩充（开关 / 蜂鸣器）**：
  - `switch`（拨动开关，自锁）：两脚 `1/2`，默认断开（`attrs.closed = 0`）；闭合时在网表中**电气透传**（两端属于同一网络），断开时两端各自成网 —— 由 `netlist.isPassThrough` 按 `closed` 动态判定，而不是像电阻那样恒透传。
  - `buzzer`（蜂鸣器）：两脚 `1(+)/2(-)`，外观由"一端被拉高、另一端被拉低"决定（复用抽取出的 `twoPinPowered` 判定，LED 也改走同一函数）。
  - 画布交互：开关整块可点击切换开合（`data-closed`），蜂鸣器随 GPIO 驱动显示鸣响（`data-on`）；面板新增两项，桥接层新增 `togglePart`（更新图纸状态 → 重建网表 → 经 `switchInjection` 向引擎写引脚电平）。
  - `switchInjection` 的电气语义：开关接到 VCC 侧时闭合与断开都读高；接到 GND 侧时闭合拉低、断开回高（与按键上拉语义一致）；无电源/地参考时默认闭合高、断开低。
- **电阻上拉/下拉语义**：网表构建改为两阶段 —— 先按导线合并得到"线段组"，再处理透传元件：电阻一端所在组直接含 VCC/GND（无电阻直连）时，另一端所在组获得 `pullUp` / `pullDown` 标记，随后合并网络并传播标记。`netPullLevel()` 对外给出 `pullup / pulldown / divider / null`；电阻分压节点（`3V3 → R1 → 节点 → R2 → GND`）会同时命中上拉与下拉，识别为 `divider`。
- **元件自动布局**：新增 `circuitStore.nextSlot()` —— 在底板右侧按五列扫描空位，返回第一个不与现有元件重叠的坐标（带 24px 边距），面板点击添加不再用固定步进 `y = 80 + (n-1) × 50` 叠加。
- **Wokwi 互导边界补强**：`diagram.json` 新增 `wokwi-slide-switch` / `wokwi-dip-switch-4` / `wokwi-buzzer` 的类型别名；拨动开关的 Wokwi 嵌套属性 `attrs.switch.position` 与内部扁平 `attrs.closed` 在导入/导出时互转（往返对称）。
- **验证**：
  - 前端单元测试 48 项通过（新增：网表 4 项开关透传 + 4 项上拉/下拉/分压语义、行为 7 项蜂鸣器/开关外观与注入、图纸 3 项开关/蜂鸣器往返与 Wokwi 别名导入）；网表 golden snapshot 保持不变。
  - UI 自动化扩为 **9 条链路**：新增「I 电路图开关」—— 放置拨动开关 → 连线 GPIO0/GND → 运行 → 点击切换 → `data-closed=1` 且注入无报错，实测通过。
  - 本轮 A（板卡 LED）/ B（画布 LED）因 QEMU 模型负载敏感失败（B 出现 Guru Meditation backtrace），与本次改动无关，属上文已记录的模型侧问题。

## 2026-09-12 · 排查并纠正"按键/开关电平无法稳定保持"的误诊

- **结论先行**：注入到 GPIO 输入引脚的电平**在模型中一直能稳定保持** —— 此前记录的"无法保持"是误诊，由三个叠加的错误观测造成；真正修掉的是一个引擎侧串口首字节丢失 bug。
- **排查过程**（模型侧 `hw/gpio/esp32_gpio.c` + 引擎侧临时打印，均已在定位后还原）：
  1. 给模型加临时打印：`set_gpio` / `GPIO_IN_REG` 读取 / 输出自环 / enable 变化全部记录。注入 GPIO0=0 一次后，3 秒观测窗口内固件对 `GPIO_IN_REG` 的**每一次读取都返回 bit0=0**（0 次读到 1），同步请求（`picsimlab_dir_pin(-1,...)`）每次都把 `forced_inputs` 里的电平成功推回 —— 保持机制本身工作正常。
  2. 双固件交叉验证：自带"GPIO4 实时镜像 GPIO0"固件（每 20ms 读一次），注入后 8 秒 264 次采样 GPIO4 全为高、零抖动；真实 demo 固件（LED 闪烁 + BOOT 按键），注入后等待 1.5 秒（让固件走完当前闪烁周期），之后 3 秒内 LED 常亮、零低采样 —— 固件持续读到低电平。
- **误诊的三重原因**：
  1. **固件产物被覆盖**：`build_demo/picsimlab_gpio_demo.ino.merged.bin` 早已被空草图（`void setup(){}` / `void loop(){}`）覆盖，旧 `button_injection` 测试观测到"LED 从不翻转、串口没有 BOOT pressed"，却归因到模型。
  2. **测试采样时机错误**：旧测试注入后立刻采样 LED，而固件可能正处于当前闪烁周期的低电平相位（最长约 1 秒），这段残余相位被误判为"电平丢失"。
  3. **引擎串口首字节丢失（真 bug，已修复）**：`flush_uart_if_due`（总线事件）与 `poll_uart`（轮询兜底）共用同一个消费游标。QEMU 按波特率逐字节回调，一个 "LOW\r\n" 突发跨多个 10ms flush 窗口 —— 先到的部分被总线 flush 消费并推进游标，`poll_uart` 只能拿到剩余字节，表现为**每个串口突发丢首字节**（"BOOT pressed" → "OOT pressed"，终端看似"没输出"）。
- **修复**（引擎，`src-tauri/src/sim/engine.rs`）：bus 与 poll 各自维护消费游标（`uart_bus_flushed` / `uart_polled`），互不抢占；`poll_uart` 只取"总线已发出"的部分保证不重不漏；新增 `trim_uart` 在两个游标都越过前缀后裁剪缓冲区（水位 4KB），避免内存无限增长。
- **测试**：
  - 新增 `tests/uart_poll.rs`（CI 可跑，无需 DLL）：跨 flush 窗口的首字节不丢、多轮突发有序完整、大批量字节流不重不漏、缓冲区裁剪 —— 4 个场景合一。
  - 重写 `tests/button_injection.rs`：自带真实 demo 固件编译，注入后等 1.5 秒再采样，断言 LED 全程常亮（保持生效）；`tests/gpio_hold.rs` 镜像固件用例同样断言保持。
  - 全量 Rust 测试通过（协议 6 + 工作进程 9 + 引脚 6 + 编译链 5 + 环境 4 + 工程 3 + uart_poll 1 = 34 项 CI 可跑）。
- **模型侧无需改动**：`esp32_gpio.c` 保持上游原样（调试打印已还原，DLL 已从 `build-dll/` 用同一参数重编部署，大小与部署版一致）。真正的"保持"依赖引擎在同步请求时重推外部电平（`forced_inputs`），该机制与 PICSimLab 的做法一致。

## 2026-09-12 · 元件外观升级：接入 wokwi-elements 官方原图

- **背景**：用户反馈画布上的 LED/电阻/按键等元件是手绘占位图形（简单矩形/圆形/文字），与 Wokwi / Velxio 差距明显；并指出应该用 `wokwi-elements` 元件库（Velxio 的 `frontend/package.json` 正是依赖 `@wokwi/elements@1.9.2`，circuit-muse 则是每类元件一个静态 SVG 文件）。
- **方案**：画布保持"纯 SVG"架构（平移/缩放/引脚连线/拖拽/按键按下都不动），把元件外观换成 wokwi 官方原图 —— 与 Velxio 的"HTML 绝对定位层"不同，我们选择**把 wokwi 元素 shadow DOM 里的 `<svg>` 克隆进画布 `<g>`**：
  - 新增 `src/circuit/wokwi.ts`：按元件类型实例化 wokwi 元素（缓存实例、挂到隐藏宿主保证 Lit 首渲染），把我们的视觉状态映射成元素属性（LED `value/color`、电阻 `value`、按键 `pressed`、开关 `value`、蜂鸣器 `hasSignal`、电位器 `value(0..1023)`），取回 shadow DOM 的 `<svg>` 并归一化尺寸。
  - 新增 `src/components/WokwiPart.tsx`：状态变化时异步替换 `<g>` 内的 wokwi 原图；克隆节点不携带 wokwi 元素自身的事件处理器，因此画布的连线/拖拽/按下交互完全不受影响。
  - `catalog.ts` 的尺寸与引脚坐标全部对齐 wokwi 原图（LED 40×50、电阻 59×11、按键 67×45、拨动开关 32×35、蜂鸣器 64×76、电位器 76×76，引脚位置取自 wokwi `pinInfo`），连线端点正好落在原图引脚上。
  - 电位器拖动热区改为透明矩形覆盖原图，旋钮转动由 wokwi 元素按 value 渲染；按键按下/开关切换仍由原有交互驱动，外观由 `pressed`/`value` 属性实时反映。
- **验证**：
  - `tsc` 与 `vite build` 通过（bundle 增至约 694KB，含 wokwi-elements 约 440KB，桌面应用可接受）。
  - 前端单元测试 48 项全部通过（元件目录/网表/行为/图纸不受影响，引脚坐标变更后仍满足"坐标在元件范围内"）。
  - UI 自动化新增断言：LED/按键/电位器/拨动开关四类元件放置后必须出现 `.wokwi-art svg`（即 wokwi 原图已渲染），否则判定失败。
- **说明**：拨动开关用 wokwi-slide-switch 的 1/2 两脚（原图三脚，第 3 脚仅展示不参与接线）；底板（ESP32 DevKitC 竖排引脚）保持自绘，因为其引脚表与 Rust 侧 pinmap 绑定，wokwi 的横排板布局不适用。

## 2026-09-13 · 界面整体升级：VS Code 风格深色工作台

- **背景**：用户希望整体界面照抄 Velxio 或 circuit-muse。调研两仓库后发现二者是**同一套设计语言 —— VS Code 风格深色 IDE**（44px 毛玻璃顶栏 + 38px 统一工具条 + 编辑器/画布分栏 + 底部终端面板；`#1e1e1e/#252526` 中性灰阶、蓝主色 `#007acc/#0071e3`、终端绿字黑底），差异仅在细节。故按其设计语言重做壳层，保留全部功能与自动化契约。
- **改动**：
  - `App.css` 全面重写：VS Code 设计令牌（灰阶/蓝/终端绿），毛玻璃顶栏 `app-header`（44px，blur+saturate）、统一工具条 `.toolbar`（38px）、可拖拽分栏 `.splitter`（`--split` 变量控制 22%–75%）、底部终端面板（绿字黑底 + 运行脉冲指示灯）、面板标题/标签页/按钮/下拉/环境面板全部按新主题重排。
  - `App.tsx`：工程工具条（新建/打开/保存/最近/导入导出/恢复/环境自检）上移到顶栏左侧品牌旁；编辑器/画布之间加入 5px 可拖拽分隔条；其余布局与状态逻辑不变。
  - 保留全部类名与 `data-*` 属性契约（`.toolbar`/`.pane-tabs .tab`/`.btn-run`/`.btn-stop`/`.status`/`.msgbar`/`[data-action]` 等），UI 自动化无需改动。
- **验证**：`tsc` 通过、前端 65 项单元测试全部通过；UI 自动化跑电路阶段验证选择器兼容性与元件交互。

---

## 关键决策一览

| 决策 | 结论 | 原因 |
| --- | --- | --- |
| 仿真引擎复用方式 | 复用 PICSimLab 的 `libqemu-xtensa.dll`，Rust 侧 FFI 封装 | 已有可用构建，无需重造仿真轮子 |
| 固件编译参数 | 固定 `dio / 40MHz / 4MB` | 80MHz/QIO 固件在 QEMU 下 flash 初始化失败 |
| 仿真运行位置 | **独立子进程** `esp32-sim` | QEMU DLL 不支持同进程二次初始化、不可安全卸载 |
| 启动成功判据 | 引脚活动持续（≥4 次、跨度 ≥3 秒、最近 2 秒内仍活动）或 UART ≥1500B | 排除 ROM 冷启动卡死的误判 |
| 桌面技术栈 | Tauri 2（Rust 后端 + Web 前端） | 体积小、Rust 可直接 FFI 调 DLL |
| 电路图编辑器 | 自研 SVG 画布 + 复用 wokwi-elements，格式对齐 `diagram.json` | 官方仅开源组件，编辑器需自研（见设计方案 7.3） |
| 工程文件格式 | 单个 `.fmp`（JSON，内含 `diagram.json` 文本） | 电路语义留在前端，Rust 侧只负责存取 |
| 自动保存位置 | 应用数据目录 `autosave.fmp`，每 30 秒一次 | 不污染用户工程目录，异常退出后可恢复 |
| CI 运行范围 | 只跑不需要 QEMU 的用例，DLL 用例 `#[ignore]` | 仓库不含大体积二进制，CI 无法提供 DLL |

## 当前状态

- **已完成（M1 MVP）**：代码编辑 → 一键编译 → 本地仿真 → 板卡/串口可视化，全链路可用并已自动化验证。
- **已完成（M2 第一批 + 续）**：电路数据模型与网表、SVG 画布、`diagram.json` 读写、LED/电阻/按键/电位器/拨动开关/蜂鸣器六类元件、电阻上拉/下拉语义、元件自动布局、Wokwi 互导边界补强。
- **进行中（M3 第一批）**：工程文件与 Wokwi zip 互导、环境自检面板、CI 流水线（已完成）；协议级测试矩阵（已完成）。
- **可用命令**：
  - `npm run build:release` — 一键构建（exe + 安装包）
  - `npm run test:unit` — 前端单元测试（网表/行为/图纸/工程文件）
  - `npm run test:rust` — Rust 测试（跳过需本机 DLL 的用例，CI 同款）
  - `npm run test:rust:local` — Rust 全量测试（含 DLL 用例，需先准备 lib/qemu 与固件）
  - `npm run test:ui` — UI 自动化验证（真实鼠标事件，9 条链路）
- **已知限制**：
  - 宿主负载高（如远程桌面/录屏占用 CPU）时，QEMU 冷启动偶发卡死；应用会自动重试，仍失败则提示重新运行。
  - 产品名/进程名仍为 `esp32-ide`（文件夹与仓库已改为 FengtouMu）。
  - `lib/qemu` 等大体积二进制不入库，首次构建需按 `docs/build-qemu-dll-windows.md` 自行编译或从本地拷贝。
  - 此前记录的"按键/开关注入电平无法保持"已经查实为误诊（观测固件被空草图覆盖 + 测试采样时机错误 + 引擎串口首字节丢失），见 2026-09-12 排查章节；模型侧保持机制工作正常。

## 后续计划

- **M3（续）**：界面文案国际化、元件扩展（LCD1602、舵机）、引脚时序仪器雏形。
- **M4**：中文教学示例库、引导式教程、一键导出/分享。

## 2026-09-13 界面整体复刻 CircuitMuse 编辑器壳层

- 按用户要求，编辑器界面整体照抄 `circuit-muse`（VS Code 风格深色壳），不只换色，布局/文案/交互结构一致（顶栏统一工具条 38px、Code/Both/Circuit 三态视图、文件树 EXPLORER、文件标签、输出控制台、画布头部 Serial/Add/缩放、串口监视器 115200 baud）。
- CSS 逐字复用自 `circuit-muse`（App.css/EditorToolbar/FileTabs/FileExplorer/SimulatorCanvas/ComponentPalette + Inter/JetBrains Mono 字体），文案取 circuit-muse 的英文原文（editor.shell.* 等键值）；运行时状态提示（消息条）保留中文（自动化断言依赖）。
- 功能映射：编译/运行/停止/复位、Serial 开关、Add 元件面板、缩放 ±、More 菜单（打开/另存/导入导出 Wokwi/草图目录/fw 目录/固件镜像/环境自检/Docs/GitHub）、会话恢复横幅、Ctrl+S 保存。
- 保留全部自动化契约（.btn-run/.btn-stop/.status/.msgbar/.path、data-action、data-part 等），ui_drive.mjs 同步更新（视图切换选择器、编译按钮、环境自检需先开汉堡菜单、元件面板点击先 scrollIntoView、eval 超时放宽至 45s 以容忍 WebView2 后台节流）。
- 验证：tsc 通过、前端 65 项单元测试通过；UI 自动化 9 条链路中 8 条通过（A 板卡 LED 37 翻转、B 画布 LED 39 翻转、C 按键、D 电位器 0.5→1、E 恢复、F 新建、G 环境自检 8 项、H 一键编译）；唯一失败 I 为 QEMU 模型第 5 次启动仿真的已知负载崩溃（页面失联），与界面无关。
- 调试要点：① 元件面板 6 条目在矮窗口下被滚动容器裁剪，电位器条目中心落到 help 区 → 压缩面板 + 点击前 scrollIntoView；② 编译期间 WebView2 后台节流使 CDP eval 挂起超 15s → 自动化超时放宽至 45s。

## 2026-09-13 元件选择器 + 输出控制台默认展开（复刻 CircuitMuse 交互）

- 按用户要求补两处 CircuitMuse 交互：① 编译输出控制台（Output Console）默认展开，位于代码编辑器下方（consoleOpen 默认 true）；② Add 按钮改为弹出「Add Component」元件选择器模态框（搜索栏 + 分类标签 + wokwi 实时缩略图卡片网格 + footer 计数），点击卡片即按 nextSlot 落位添加到画布并自动切到电路视图，ESC/点击遮罩关闭；CSS 逐字复用 ComponentPickerModal.css。
- 删除画布停靠式元件面板（PartPalette），其删除元件/删除导线/清空/导入导出 diagram.json 并入 More ⋯ 菜单（带禁用态与分隔线）。
- 自动化同步：新增 addPartViaPicker（点 Add → 点卡片），B/C/D/I 四阶段改用该流程；全量验证 7/9 通过（C/D/E/F/G/H/I 含选择器流程与编译），A/B 失败仍为 QEMU 模型 Guru Meditation（EXCCAUSE 7）已知崩溃及其级联，与本次改动无关。

## 2026-09-13 编译无黑框 + 按钮图标 + 项目管理照抄（复刻 CircuitMuse）

- 编译不再弹黑框：buildchain.rs 两处 arduino-cli 子进程（version 探测 + compile）加 Windows 专用 no_console（creation_flags=CREATE_NO_WINDOW 0x08000000，#[cfg(windows)] 条件编译，非 Windows 走空实现）。
- 编译按钮样式照抄：EditorToolbar 编译按钮图标从双箭头换成 CircuitMuse 的 wrench（M14.7 6.3a1…，18×18），运行改 fill polygon（5,3 19,12 5,21）、停止改 fill rect（3,3 18,18 rx2）、复位改 9 宫格圆弧（M3 12a9…+M3 3v5h5），全部 18×18，颜色保持 .tb-btn-compile 天蓝 #4fc3f7。
- 项目管理照抄：新增 src/components/ProjectsModal.tsx（UI/文案/交互逐字复用 CircuitMuse ProjectsModal —— Projects 标题、New Project、Import .vlx / .zip、列表项点击打开、✕ 删除并 confirm）；数据对接本地 recent 列表；溢出菜单顶部新增 Projects… 入口；补 Ctrl+S 保存快捷键（照抄 handleSaveClick 逻辑）。
- projectStore 新增 removeRecent（从最近列表移除并持久化，不删磁盘文件）。
- 验证：tsc 通过、65 项单元测试通过、cargo check/build 通过；CDP 断言编译按钮 = wrench 图标 + #4fc3f7；ProjectsModal 打开正常（标题/按钮/列表齐全）；一键编译链路成功（arduino-cli 无窗口产出 merged.bin）。

## 2026-09-13 修复编译冻结 UI：cmd_compile 改 async + spawn_blocking

- 根因：cmd_compile 是**同步** #[tauri::command]，Tauri 2 中不带 async 的命令默认在**主线程**执行；内部 Command::output() 阻塞等待 arduino-cli 编译 1-3 分钟，整个 WebView 事件循环被冻结 → 点击编译后界面完全无法操作。
- 修复：cmd_compile 改为 sync fn，编译逻辑包进 	auri::async_runtime::spawn_blocking（阻塞线程池），主线程只等待结果，UI 保持可交互；编译按钮 busy 禁用逻辑不变。
- 验证：CDP 实机确认编译进行中（busy=true）时，视图切换 / More 菜单 / ProjectsModal 打开关闭均正常响应（总耗时 1.1s），编译完成后正常产出 merged.bin。

## 2026-09-13 本地项目库（照抄 CircuitMuse 项目管理体验）

- 把"最近列表 + 单文件"升级为真正的本地项目库：应用数据目录 projects/ 下每个工程一个 .fmp（复用现有格式），Rust 新增 cmd_library_list（扫描目录按 updatedAt 倒序返回元数据）/ cmd_library_delete（防路径穿越按 id 删文件），注册进 invoke_handler。
- 前端 projectStore 增加 library 列表与 libraryId（当前工作区在库中的身份）：编辑停止 2s 防抖自动写库（照抄 CircuitMuse autoSaveCurrentState 2000ms debounce）；新建 / 打开外部 .fmp / 另存为 / 导入 Wokwi / 恢复会话都会分配新库 id 并登记入库；saveToLibrary / openFromLibrary / deleteFromLibrary / efreshLibrary 完整闭环。
- ProjectFile 增加可选 createdAt 字段（Rust Project 同步补 created_at，serde default 兼容旧文件）。
- ProjectsModal 改为项目库数据源：显示工程名 + Updated 日期 + current 高亮，点击打开、✕ 删除（确认后删文件并刷新）；New Project / Import .vlx / .zip 照旧。
- 验证：tsc / 65 单测 / cargo check / release 构建全过；CDP 实机确认：新建即入库、放元件防抖 2s 自动入库、列表 current 高亮、打开库工程成功、删除条目后列表与后端文件同步减少。

## 2026-09-13 工程格式转正：.vlx / Wokwi .zip 一等公民，.fmp 退役

- 按用户要求，.vlx（自包含快照）与 Wokwi .zip 成为工程格式一等公民，.fmp 不再作为保存格式。
- .vlx 设计：源码文件列表 iles[]（名称 + 内容内嵌）+ diagram（电路图文本内嵌）全部自包含，路径字段（sketchDir/fwDir/flashPath）降级为可选的本机加速信息，不再参与内容比较（sameProjectContent 只比较 files/code/diagram/fqbn）。format 标识 engtoumu-project。
- 兼容读取：① 旧 .fmp（无 format 字段、无 files 数组的单文件结构）② velxio / circuit-muse 的 .vlx（format: velxio-project / circuit-muse-project，从 fileGroups 平铺提取源码）—— 打开旧文件不丢数据，但保存一律写 .vlx。
- Rust 侧：Project 增加 format/files/createdAt 字段（serde default 兼容）；autosave.fmp → autosave.vlx；项目库目录从 *.fmp 改为 *.vlx（list/delete 同步）；cmd_library_list 元数据解析不变。
- 前端：PROJECT_EXT=fmp → vlx；打开工程过滤器（.vlx 主 + .fmp 兼容 + .zip + .json，zip 走 importWokwiProject）；另存为固定 .vlx；项目库保存/打开路径 .vlx；FileExplorer 显示 .vlx。
- 自动化：ui_drive.mjs autosave 预置改 autosave.vlx + fengtoumu-project 结构；Rust project_io 测试改 .vlx 并断言 files 往返。
- 验证：tsc / 69 单测（新增 velxio vlx 兼容、旧 fmp 兼容、附加源码比较等）/ cargo test project_io 3 项 / release 构建全过；CDP 实机确认新建即登记 .vlx 库条目、.vlx 内容自包含（files + diagram 内嵌）、autosave.vlx 生成、Projects 只列 .vlx。
- 清理：删除旧库目录 *.fmp 测试残留与旧 autosave.fmp。

## 2026-09-13 移除 .fmp 兼容 + 新建工程交互照抄 velxio/circuit-muse

- 用户处于开发阶段，不再需要 .fmp 兼容：parseProjectFile 缺少 format 字段直接拒绝（提示旧 .fmp 不再支持）；打开工程对话框移除 .fmp 选项；README/注释同步更新；单测改为"缺 format 拒绝"。
- 新建工程交互照抄 velxio/circuit-muse：有未保存改动时弹 confirm 确认（Start a new project? This clears every component, wire and file. This cannot be undone.）→ 停止仿真（幂等）→ 清空电路图 → 重置默认 Blink 源码 → 分配新库条目登记；用户取消时不执行也不关闭 Projects 弹窗。
- 关键坑：Tauri 2 的 window.confirm 返回 Promise（原生对话框异步），不 await 会导致确认被跳过；newProject 改 async 返回 boolean；ProjectsModal handleNew 仅在真正新建后 onClose。
- 自动化：ui_drive.mjs 注入 window.confirm 桩（CDP 无法处理 Tauri 原生对话框）。
- 验证：tsc / 69 单测 / release 构建全过；CDP 实机确认 confirm 调用 2 次：第一次取消（元件保持 2 个、弹窗不关），第二次确认（元件清为 1、源码重置为默认）。

## 2026-09-13 保存交互照抄 velxio/circuit-muse

- 用户确认照抄参考项目的保存行为：点 Save / Ctrl+S 总是弹出保存对话框，让用户选择位置导出 .vlx（saveProject 直接委托 saveProjectAs，不再静默覆盖当前路径）。
- 平时编辑仍由 2s 防抖自动保存到本地项目库（对应 circuit-muse 的 IndexedDB 自动备份）——两套并存，与参考项目一致：自动备份无感，显式 Save 才导出文件。
- 验证：tsc / 69 单测 / release 构建全过；CDP 冒烟确认应用启动正常、Save project as… 与 Projects… 菜单入口齐全。

## 2026-09-13 电路图底板换用 wokwi 官方 esp32-devkit-v1 原图

- 用户反馈底板太丑：此前电路图里的底板（board-devkitc）是手绘的蓝色矩形 + 文字，未走 wokwi-elements 官方元件（当时因 38 引脚自定义体系与官方 30 引脚不兼容而保留手绘）。
- 修复：wokwi 接入层 WOKWI_TAG 增加 board-devkitc → wokwi-esp32-devkit-v1；applyWokwiState 支持底板（led1 板载 LED 亮灭、ledPower 常亮）；WOKWI_SIZE 底板按 38 引脚布局矩形（210×624），克隆时 preserveAspectRatio="none" 拉伸铺满；CircuitCanvas 底板分支改渲染 WokwiPart 官方图 + 虚线选中框 + 保留 38 引脚圆点（连线/交互不变）。
- 引脚几何与连线体系不变，仅外观替换；板载 LED（GPIO2）状态实时映射到官方元件的 led1。
- 验证：tsc / 69 单测 / release 构建全过；CDP 实机确认底板 .wokwi-art 渲染官方 svg（viewBox 107x201、15 path / 24 rect / 13KB 内容），38 引脚圆点完整保留。

## 2026-09-13 连线体系对齐 velxio/circuit-muse：Wokwi 引脚命名 + 信号着色 + 拐点 + 自动布线

- 按用户要求，连线体系照抄 velxio/circuit-muse：
  ① 引脚命名：底板 38 引脚改用 Wokwi 官方 esp32-devkit-v1 命名（GPIO2→D2、GPIO0→D0、GPIO36→VP、GPIO39→VN、GPIO16/17→RX2/TX2、GPIO1/3→TX0/RX0，其余 GPIO→D 数字）；旧 GPIO 名经 GPIO_LEGACY_ALIASES 兼容导入（旧工程文件与自动化脚本不受影响），导出统一写 Wokwi 名。
  ② 信号着色：新增 catalog GPIO_SIGNAL 表（UART/I2C/SPI/ADC 按 GPIO 归类）+ WIRE_COLORS（逐字照抄 velxio wireColors.ts：VCC 红/GND 黑/模拟蓝/数字绿/PWM 紫/I2C 金/SPI 橙/UART 青）；连线时按两端信号优先级合并着色（照抄 determineSignalType）。
  ③ 拐点：Connection 增加 waypoints 字段（对齐 Wokwi diagram.json 第 4 项 [[x,y],...] 格式），双击线段插入正交拐点（投影对齐线段所在轴），序列化/解析贯通。
  ④ 自动布线：新增 src/circuit/wire.ts，创建连线时自动生成 L/Z 型正交折线并绕开其他元件包围盒（轻量版参考 velxio wireAutoRoute 思想）。
- 同步：测试全部改 Wokwi 引脚名（netlist/diagram/behavior/snapshot/projectStore），快照重生成；ui_drive.mjs 选择器与种子数据同步（esp:D2/esp:D0/esp:D34/esp:VP…）。
- 验证：tsc / 69 单测 / cargo test 9 项 / release 构建全过；CDP 实机确认：底板引脚已全部 Wokwi 命名且旧 GPIO 名消失；LED→D2 连线绿色（digital）、电位器→D34 连线蓝色（analog）；新连线自动生成正交路径（M…L…L 折线）；双击线段可插入拐点且路径变化。

## 2026-09-13 自动布线升级为完整 A* 迷宫算法（移植 velxio wireAutoRoute）

- 按用户要求，把轻量 L/Z 型布线升级为 velxio 的完整 A* 迷宫布线：src/circuit/wire.ts 全量重写，逐字移植 wireAutoRoute.ts 的算法 —— 在压缩正交网格上 A* 寻路，硬避开元件包围盒（端点所在矩形自动开逃生走廊 carveEscape / unionEscapeDir），对并行贴线（OVERLAP_PENALTY_PER_PX）、垂直交叉（CROSS_PENALTY）、90° 拐弯（BEND_PENALTY）施加软代价，因此新线会绕开其它元件 AND 既有导线（不再只是绕元件）。
- 渲染同步升级：移植 wireUtils.ts 的 roundedPathFromPoints / expandOrthogonalPoints / simplifyOrthogonalPath / fuseMicroJogs / generateOrthogonalPath —— 连线为圆角正交折线（Wokwi 风格），微抖动自动融合。
- 高层封装：autoRoute(diagram, from, to) 收集元件包围盒 + 全部既有导线段，生成规范化存储拐点；clickPin 创建连线时调用。
- 新增 wire.test.ts 5 项：无障碍退化、绕障碍、绕既有导线、面向 Diagram 生成、正交性。
- 验证：tsc / 74 单测 / release 构建全过；CDP 实机确认连线渲染为圆角正交折线（M…L…Q…L，水平→圆角→垂直），信号着色正常。

## 2026-09-14 修复运行仿真时弹黑框（esp32-sim 子进程加 CREATE_NO_WINDOW）

- 用户反馈：点"运行"会弹出一个黑色控制台窗口。根因：编译（arduino-cli）之前已加 CREATE_NO_WINDOW，但运行仿真子进程 esp32-sim.exe 的 spawn_child 漏了 —— Windows 下控制台程序被 spawn 时会创建新控制台窗口。
- 修复：worker_host.rs spawn_child 加 #[cfg(windows)] creation_flags(0x08000000)（CREATE_NO_WINDOW），stdin/stdout 管道不受影响（仿真通信照常）。
- 验证：release 构建全过；实机确认点运行后 esp32-sim 进程存在、MainWindowHandle=0（无窗口）、仿真正常进入运行中。
